# 树与二叉搜索树

> **一句话**：树是层次结构；BST 保证左 < 根 < 右，支持有序查找。
>
> 关键词：`二叉树` `BST` `遍历` `前序` `中序` `后序` `层序`

[← 数据结构索引](study_note/data_struct/index.html) · [速查表](md.html?file=study_note/data_struct/速查.md)

---

## 速查卡

| 项目 | 内容 |
|------|------|
| 二叉树 | 每节点最多两个孩子 |
| BST 性质 | 左子树 < 根 < 右子树（递归定义） |
| BST 查找/插/删 | O(h)，h 为树高 |
| 平衡 BST | h = O(log n)，如 `std::map` |
| 退化 BST | 链表形，h = n |

---

## 二叉树

### 节点定义

```cpp
// === File: binary_tree.cpp ===
struct TreeNode {
    int val;
    TreeNode* left;
    TreeNode* right;
    explicit TreeNode(int x) : val(x), left(nullptr), right(nullptr) {}
};
```

### 四种遍历

| 遍历 | 顺序 | 典型用途 |
|------|------|----------|
| 前序 | 根 → 左 → 右 | 复制树、前缀表达式 |
| 中序 | 左 → 根 → 右 | BST 得有序序列 |
| 后序 | 左 → 右 → 根 | 删除树、后缀表达式 |
| 层序 | 逐层从左到右 | BFS，用队列 |

### 递归遍历

```cpp
void preorder(TreeNode* root, std::vector<int>& out) {
    if (!root) return;
    out.push_back(root->val);
    preorder(root->left, out);
    preorder(root->right, out);
}

void inorder(TreeNode* root, std::vector<int>& out) {
    if (!root) return;
    inorder(root->left, out);
    out.push_back(root->val);
    inorder(root->right, out);
}
```

### 层序遍历（BFS）

```cpp
#include <queue>

std::vector<std::vector<int>> levelOrder(TreeNode* root) {
    std::vector<std::vector<int>> res;
    if (!root) return res;
    std::queue<TreeNode*> q;
    q.push(root);
    while (!q.empty()) {
        int sz = static_cast<int>(q.size());
        std::vector<int> level;
        for (int i = 0; i < sz; ++i) {
            TreeNode* node = q.front(); q.pop();
            level.push_back(node->val);
            if (node->left) q.push(node->left);
            if (node->right) q.push(node->right);
        }
        res.push_back(level);
    }
    return res;
}
```

### 数组表示（完全二叉树）

下标 `i` 的左子 `2i+1`，右子 `2i+2`，父 `(i-1)/2`。堆用此表示。

---

## 二叉搜索树（BST）

### 性质

对任意节点：左子树所有值 < 节点值 < 右子树所有值。

### 查找

```cpp
TreeNode* searchBST(TreeNode* root, int target) {
    while (root) {
        if (target == root->val) return root;
        root = target < root->val ? root->left : root->right;
    }
    return nullptr;
}
```

### 插入

```cpp
TreeNode* insertBST(TreeNode* root, int val) {
    if (!root) return new TreeNode(val);
    if (val < root->val) root->left = insertBST(root->left, val);
    else if (val > root->val) root->right = insertBST(root->right, val);
    return root;
}
```

### 删除（三种情况）

1. 无子或一子：用子替换
2. 两子：用**中序后继**（右子树最小）替换，再删后继

```cpp
TreeNode* minNode(TreeNode* node) {
    while (node->left) node = node->left;
    return node;
}

TreeNode* deleteBST(TreeNode* root, int key) {
    if (!root) return nullptr;
    if (key < root->val) root->left = deleteBST(root->left, key);
    else if (key > root->val) root->right = deleteBST(root->right, key);
    else {
        if (!root->left) { TreeNode* r = root->right; delete root; return r; }
        if (!root->right) { TreeNode* l = root->left; delete root; return l; }
        TreeNode* succ = minNode(root->right);
        root->val = succ->val;
        root->right = deleteBST(root->right, succ->val);
    }
    return root;
}
```

### STL 有序映射

```cpp
#include <map>
std::map<int, std::string> m;  // 红黑树，O(log n)
m[1] = "a";
auto it = m.find(1);
```

---

## 复杂度

| 操作 | 平均（平衡） | 最坏（退化） |
|------|--------------|--------------|
| 查找 | O(log n) | O(n) |
| 插入 | O(log n) | O(n) |
| 删除 | O(log n) | O(n) |

---

## 典型应用

- 有序集合、范围查询
- 表达式树
- 文件系统目录树
- 决策树、语法树

---

## 例题：验证 BST

```cpp
bool validate(TreeNode* root, long long lo, long long hi) {
    if (!root) return true;
    if (root->val <= lo || root->val >= hi) return false;
    return validate(root->left, lo, root->val) &&
           validate(root->right, root->val, hi);
}

bool isValidBST(TreeNode* root) {
    return validate(root, LLONG_MIN, LLONG_MAX);
}
```

---

## 迭代遍历（栈模拟）

中序迭代（BST 得有序序列）：

```cpp
std::vector<int> inorderIter(TreeNode* root) {
    std::vector<int> res;
    std::stack<TreeNode*> stk;
    TreeNode* cur = root;
    while (cur || !stk.empty()) {
        while (cur) { stk.push(cur); cur = cur->left; }
        cur = stk.top(); stk.pop();
        res.push_back(cur->val);
        cur = cur->right;
    }
    return res;
}
```

---

## 平衡树与 STL

退化 BST 性能差；工程有序用 `std::map`。手写平衡见 [AVL与平衡树](md.html?file=study_note/data_struct/14_AVL与平衡树.md)。

---

## 易错点

| 易错 | 正确做法 |
|------|----------|
| 只比较父子 | BST 需整个左/右子树范围约束 |
| 中序忘记判空 | 递归基 `if (!root) return` |
| 删除两子节点 | 用后继或前驱值替换 |
| 与堆混淆 | 堆是结构性质；BST 是序性质 |

---

## 关联知识

- [堆](md.html?file=study_note/data_struct/08_堆.md) — 数组存完全二叉树
- [栈与队列](md.html?file=study_note/data_struct/05_栈与队列.md) — 层序用队列
- [图](md.html?file=study_note/data_struct/09_图.md) — 树是无环连通图
